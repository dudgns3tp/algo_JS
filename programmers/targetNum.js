function solution(numbers, target) {
    var answer = 0;
    var minus = -1;
    var len = numbers.length;
    var bucket;
    for(let i=0; i<len; i++){
        for(let j=)
    }

    return answer;
}
var arr = [1,1,1,1,1];
//solution(arr, 3);
var cnt = arr.length;
console.log(cnt);